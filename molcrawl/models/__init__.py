"""Model architecture implementations (decoder / encoder) by family.

Sub-packages implement specific architectures (GPT-2, BERT, ChemBERTa-2,
ESM-2, DNABERT-2, RNAformer). Architecture-specific data handling (e.g.
tokenizers) lives in :mod:`molcrawl.data`; this package is intentionally
kept modality-agnostic where possible.
"""
